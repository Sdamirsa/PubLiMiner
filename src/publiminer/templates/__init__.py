"""Bundled templates scaffolded by `publiminer setup`.

The YAML files here ship inside the wheel (see the
``[tool.hatch.build.targets.wheel.force-include]`` section of
``pyproject.toml``) and are read via ``importlib.resources`` so the
same code path works in a source checkout and in an installed wheel.
"""

from __future__ import annotations
