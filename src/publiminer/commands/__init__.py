"""Standalone subcommands that aren't full pipeline steps.

`setup` is the first-run interactive wizard (email + NCBI key + scaffolding).
Pipeline steps live under `publiminer.steps/*`; these are utility commands.
"""

from __future__ import annotations
